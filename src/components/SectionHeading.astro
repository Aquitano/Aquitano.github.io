---
interface Props {
    index: string;
    title: string;
}

const { index, title } = Astro.props;
---

<div class="border-line mb-10 flex items-baseline gap-4 border-t pt-5 md:mb-16 md:gap-6 lg:gap-8">
    <span class="meta text-accent shrink-0 tabular-nums lg:w-14">/{index}</span>
    <h2 class="display text-[clamp(1.75rem,4.2vw,3rem)] leading-[1.05]">{title}</h2>
    <span class="bg-line hidden h-px flex-1 self-center sm:block" aria-hidden="true"></span>
</div>
